---
phase: 03-expiry-sweeper
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - vercel.json
autonomous: true
requirements:
  - EXP-04
must_haves:
  truths:
    - "The Vercel Cron job is configured to call /api/cron/release-expired every minute"
  artifacts:
    - path: "vercel.json"
      provides: "Vercel cron scheduling configuration"
  key_links:
    - from: "vercel.json"
      to: "/api/cron/release-expired"
      via: "cron schedule"
      pattern: "\"cron\": \"* * * * *\""
---

<objective>
Configure the Vercel Cron scheduler to trigger the expiry sweeper endpoint automatically every minute.

Purpose: Automate the release of expired reservations without manual intervention.
Output: Updated vercel.json with the correct cron schedule.
</objective>

<execution_context>
@/Users/suryaps/Documents/Hackathons/allohealth/allo-reservation-system/.claude/get-shit-done/workflows/execute-plan.md
@/Users/suryaps/Documents/Hackathons/allohealth/allo-reservation-system/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-expiry-sweeper/03-CONTEXT.md
@vercel.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Vercel Cron Schedule</name>
  <files>vercel.json</files>
  <action>
    Update or create `vercel.json` to include the `crons` configuration.
    - Path: `/api/cron/release-expired`
    - Schedule: `"* * * * *"` (Every minute)
    - Ensure existing configurations in `vercel.json` are preserved.
  </action>
  <verify>
    <automated>grep "\"cron\": \"\* \* \* \* \*"\" vercel.json</automated>
  </verify>
  <done>
    `vercel.json` is correctly configured to trigger the sweeper endpoint every minute.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel Infrastructure → API Route | Trusted trigger for the cron job |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Denial of Service | vercel.json | accept | Vercel's cron infrastructure prevents excessive triggers beyond the configured schedule. |
| T-03-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- Verify that `vercel.json` contains the `crons` array with the correct path and schedule.
</verification>

<success_criteria>
- Vercel configuration is updated to run the expiry sweeper every minute.
</success_criteria>

<output>
Create `.planning/phases/03-expiry-sweeper/03-02-SUMMARY.md` when done
</output>
